import {InnerPage} from "@/components/sections/inner-page";import {pages} from "@/config/pages";export const metadata={title:"Contact"};export default function Page(){return <InnerPage {...pages.contact}/>}
