import type {Metadata} from "next";
import Link from "next/link";
import {notFound} from "next/navigation";
import {ArrowLeft,ArrowRight,CheckCircle2} from "lucide-react";
import {services} from "@/config/site";
export function generateStaticParams(){return services.map(service=>({slug:service.slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const service=services.find(item=>item.slug===slug);return service?{title:service.title,description:service.short}:{};}
export default async function ServiceDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const service=services.find(item=>item.slug===slug);if(!service)notFound();return <main className="relative min-h-screen overflow-hidden pb-24 pt-36"><div className="grid-bg absolute inset-0 -z-10"/><section className="container"><Link href="/services" className="inline-flex items-center gap-2 text-sm text-slate-400 hover:text-cyan-200"><ArrowLeft className="size-4"/> All services</Link><p className="eyebrow mt-12">{service.code}</p><h1 className="mt-6 max-w-5xl text-5xl font-semibold tracking-[-.05em] sm:text-6xl lg:text-8xl">{service.title}</h1><p className="mt-8 max-w-3xl text-xl leading-9 text-slate-300">{service.summary}</p></section><section className="container mt-16 grid gap-4 lg:grid-cols-[1.1fr_.9fr]"><div className="glass rounded-3xl p-8 md:p-10"><p className="eyebrow">Representative scope</p><div className="mt-8 grid gap-4 sm:grid-cols-2">{service.points.map(point=><div key={point} className="flex items-start gap-3 rounded-2xl border border-white/5 bg-white/[.025] p-4"><CheckCircle2 className="mt-0.5 size-5 shrink-0 text-cyan-300"/><span>{point}</span></div>)}</div><p className="mt-8 text-sm leading-6 text-amber-200/70">The exact deliverables, governing codes, equipment, personnel certifications and exclusions must be confirmed for each client scope.</p></div><aside className="rounded-3xl border border-cyan-200/15 bg-gradient-to-br from-cyan-300/10 to-transparent p-8 md:p-10"><p className="eyebrow">Plan the assignment</p><h2 className="mt-5 text-3xl font-semibold">Tell us what must be inspected, verified or improved.</h2><p className="mt-5 leading-7 text-slate-400">Prepare the asset type, site, applicable standard, project stage, schedule and available technical documents.</p><Link href="/contact" className="mt-8 inline-flex items-center gap-2 rounded-full bg-cyan-300 px-5 py-3 font-semibold text-slate-950">Request a discussion <ArrowRight className="size-4"/></Link></aside></section></main>}
