"use client";
import Link from "next/link";import {Menu,X} from "lucide-react";import {AnimatePresence,motion} from "motion/react";import {useState} from "react";import {navigation} from "@/config/site";
export function Navigation(){const[open,setOpen]=useState(false);return <header className="fixed inset-x-0 top-0 z-50 pt-4"><nav aria-label="Primary" className="glass container flex h-16 items-center justify-between rounded-2xl px-4 md:px-6"><Link href="/" className="flex items-center gap-3" aria-label="HosH home"><span className="grid size-9 place-items-center rounded-full border border-cyan-200/30 bg-cyan-300/10 font-bold text-cyan-200">H</span><span className="font-semibold tracking-[.18em]">HosH</span></Link><div className="hidden items-center gap-6 lg:flex">{navigation.map(i=><Link key={i.href} href={i.href} className="text-sm text-slate-300 transition hover:text-cyan-200">{i.label}</Link>)}<Link href="/contact" className="rounded-full bg-cyan-300 px-5 py-2.5 text-sm font-semibold text-slate-950 transition hover:bg-white">Request inspection</Link></div><button className="grid size-10 place-items-center lg:hidden" aria-label={open?"Close menu":"Open menu"} aria-expanded={open} onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></nav><AnimatePresence>{open&&<motion.div initial={{opacity:0,y:-12}} animate={{opacity:1,y:0}} exit={{opacity:0,y:-12}} className="glass container mt-2 rounded-2xl p-4 lg:hidden">{[...navigation,{label:"Contact",href:"/contact"}].map(i=><Link onClick={()=>setOpen(false)} key={i.href} href={i.href} className="block rounded-xl px-4 py-3 text-slate-200 hover:bg-cyan-300/10">{i.label}</Link>)}</motion.div>}</AnimatePresence></header>}
