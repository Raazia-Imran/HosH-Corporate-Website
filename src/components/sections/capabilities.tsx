"use client";
import Link from "next/link";
import {ArrowUpRight} from "lucide-react";
import {motion} from "motion/react";
import {services} from "@/config/site";
import {reveal} from "@/lib/motion";

export function Capabilities(){return <section className="relative py-24 md:py-36"><div className="container"><div className="mb-14 grid gap-6 md:grid-cols-2"><div><div className="eyebrow mb-5">End-to-end assurance</div><h2 className="text-4xl font-semibold tracking-[-.045em] md:text-6xl">One evidence chain.<br/>Every critical stage.</h2></div><p className="max-w-lg self-end text-base leading-7 text-slate-400">Inspection, quality management, certification and workforce capability connected across the asset lifecycle.</p></div><div className="grid gap-3 md:grid-cols-2 lg:grid-cols-3">{services.slice(0,6).map((s,index)=><motion.article variants={reveal} initial="hidden" whileInView="visible" viewport={{once:true,amount:.2}} key={s.code} className="glass group relative min-h-72 overflow-hidden rounded-3xl p-6 md:p-7"><div className="mono text-xs text-cyan-200">{s.code}</div><div className="absolute right-6 top-5 text-6xl font-light text-cyan-200/10">{String(index+1).padStart(2,"0")}</div><div className="mt-20"><h3 className="text-2xl font-medium">{s.title}</h3><p className="mt-4 leading-7 text-slate-400">{s.short}</p><Link href={`/services/${s.slug}`} className="mt-6 inline-flex items-center gap-2 text-sm font-medium text-cyan-200">View service <ArrowUpRight className="size-4 transition-transform group-hover:translate-x-1 group-hover:-translate-y-1"/></Link></div></motion.article>)}</div><div className="mt-8 text-center"><Link href="/services" className="glass inline-flex rounded-full px-6 py-3 text-sm text-cyan-100">Explore all ten service areas</Link></div></div></section>}
